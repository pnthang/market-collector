# Example env file for `scripts/setup_ubuntu.sh` and local setup.
# Copy this to one of the supported locations and fill in real values:
#  - scripts/.market_collector_env (script directory)
#  - ./ (current working directory)
#  - $HOME/.market_collector_env
#
# IMPORTANT: Do NOT commit real secrets. Keep this file as an example only.

GITHUB_PAT=ghp_xxx
DB_CONN='postgresql+psycopg2://user:pass@host:5432/db'

# API token used to protect control endpoints (set in container/env). Example only.
# If empty, auth is disabled.
API_TOKEN=example-token-CHANGE_ME

# Optional log file path used by the built-in dashboard
LOG_FILE=/var/log/market-collector.log

# Example usage (after copying and editing):
#   ./scripts/setup_ubuntu.sh
